<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"   "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!-- 定义 别名 -->
    <typeAliases>
        <!--
        单个别名的定义
        alias：别名，type：别名映射的类型  -->
        <!-- <typeAlias type="com.hust.mybatis.po.User" alias="user"/> -->
        <!-- 批量别名定义
        指定包路径，自动扫描包下边的pojo，定义别名，别名默认为类名（首字母小写或大写）
         -->
        <package name="com.jn.bean"/>

    </typeAliases>

    <mappers>
        <mapper resource="mapper/StudentMapper.xml" />
        <mapper resource="mapper/JnshuFeatureMapper.xml" />
        <mapper resource="mapper/JnshuIntroMapper.xml" />
        <mapper resource="mapper/JnshuStuShowMapper.xml" />
        <mapper resource="mapper/JnshuCareerMapper.xml" />
        <mapper resource="mapper/JnshuCareerPathMapper.xml" />
        <mapper resource="mapper/UserMapper.xml" />
        <mapper resource="mapper/PersistentLoginsMapper.xml" />
        <!--<mapper class="com.jn.mapper.StudentMapper"/>-->
    </mappers>



</configuration>